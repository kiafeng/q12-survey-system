providers = ["node"]

[phases.setup]
nixpkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install", "cd client && npm install"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = ["node", "server/index.js"]
